info:
  name: Enviar Reembolso
  type: http
  seq: 4

http:
  method: POST
  url: http://localhost:3333/reimbursements/{{reimbursementId}}/submit
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
