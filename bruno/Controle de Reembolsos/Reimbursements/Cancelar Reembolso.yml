info:
  name: Cancelar Reembolso
  type: http
  seq: 10

http:
  method: POST
  url: http://localhost:3333/reimbursements/{{reimbursementId}}/cancel
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
