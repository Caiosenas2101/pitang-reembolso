info:
  name: Pagar Reembolso
  type: http
  seq: 6

http:
  method: POST
  url: http://localhost:3333/reimbursements/{{reimbursementId}}/pay
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
