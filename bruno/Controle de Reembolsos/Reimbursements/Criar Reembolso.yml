info:
  name: Criar Reembolso
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:3333/reimbursements
  auth:
    type: bearer
    token: "{{token}}"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "categoriaId": "00000000-0000-0000-0000-000000000001",
        "descricao": "Corrida por aplicativo para visita ao cliente",
        "valor": 42.5,
        "dataDespesa": "2026-05-01"
      }

runtime:
  scripts:
    - type: after-response
      code: |-
        bru.setVar("reimbursementId", res.body.id);

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
