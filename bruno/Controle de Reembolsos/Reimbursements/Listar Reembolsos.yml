info:
  name: Listar Reembolsos
  type: http
  seq: 2

http:
  method: GET
  url: http://localhost:3333/reimbursements
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
