info:
  name: Historico Reembolso
  type: http
  seq: 7

http:
  method: GET
  url: http://localhost:3333/reimbursements/{{reimbursementId}}/history
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
