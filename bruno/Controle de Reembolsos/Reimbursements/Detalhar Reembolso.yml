info:
  name: Detalhar Reembolso
  type: http
  seq: 3

http:
  method: GET
  url: http://localhost:3333/reimbursements/{{reimbursementId}}
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
