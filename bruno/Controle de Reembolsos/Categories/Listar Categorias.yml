info:
  name: Listar Categorias
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:3333/categories
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
