info:
  name: Listar Usuarios
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:3333/users
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
